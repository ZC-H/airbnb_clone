<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">

    <!-- jQuery library -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

    <!-- Latest compiled JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>

    <title>HareBnB</title>
    <%= csrf_meta_tags %>
    
    <nav class="navbar navbar-default">
      <div class="container-fluid">
        <div class="navbar-header">
          <a class="navbar-brand" href="/">HareBnB</a>
        </div>
        <ul class="nav navbar-nav">
          <li class="active"><a href="/listings">Browse Listings</a></li>
          <li><a href="#">Page 2</a></li>
          <li><a href="#">Page 3</a></li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
            <li class="dropdown">
              <a class="dropdown-toggle" data-toggle="dropdown" href="#"><%= current_user.email %><span class="caret"></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to 'My Profile', user_path(current_user) %></li>
                <li><%= link_to 'Edit Profile', edit_user_path(current_user) %></li>
                <li><a href="#">Page 1-3</a></li>
              </ul>
            </li>
            <% if !current_user.avatar.file.nil? %>
              <li><%= image_tag current_user.avatar.url , { :size => "50", :class => "roundavatar" } %></li>
            <% end %>
            <li><%= button_to 'Sign out', sign_out_path, {:method => :delete, :class => "btn navbar-btn"} %></li>
          <% else %>
            <li><%= button_to 'Sign in', sign_in_path, { :method => :get, :class => "btn navbar-btn"} %></li>
          <% end %>
        </ul>
      </div>
    </nav>

    

    <div id="flash">
      <% flash.each do |key, value| %>
        <div class="flash <%= key %>"><%= value %></div>
      <% end %>
    </div>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
