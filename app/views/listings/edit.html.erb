<h1>Edit Listing</h1>

<%= form_for @listing, :method => :put do |f| %>
	<%= render partial: "form", :locals => {:f => f} %>

  <p>
    <%= f.submit "Submit Listing", :class => 'btn btn-primary btn-lg' %>
  </p>
    
	
<% end %>